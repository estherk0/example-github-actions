name: Ghost Inspector

on:
  release:
    types: [prereleased]

jobs:
  sleep:
    runs-on: ubuntu-latest
    name: nothing
    steps:
    - name: Sleep for 30 seconds
      uses: bash
      run: |
        echo "prereleased"
